递归和栈的讲解

第一：递归(recursion)：
       递归的组成：
          基线条件（base case）:函数不再调用自己，跳出循环
          递归条件(recursive case)：函数调用自己
第二：栈（stack）先进后出的数据结构为栈（FILO）
        栈的两种操作：
           压入（插入）
           弹出（删除并读取）